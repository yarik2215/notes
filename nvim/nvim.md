# Nvim

## What I want from IDE

* Some git integration:
  * view blame and file history
  * view diff
  * apply hunks (resolve conflicts interactively)
* Run tests (neotest)
* Debug app (nvim-dap)
* Project structure (fzf, neotree)
* code highlights (treesitter, lsp-servers)
* some AI tooling? (aider)
